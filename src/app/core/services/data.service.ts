import { Injectable } from '@angular/core';


// Questo servizio è un placeholder per le chiamate al backend per il recupero dei dati, 
// come la lista dei videogiochi, i dettagli di un videogioco, ecc.

@Injectable({
  providedIn: 'root',
})
export class Data {
  
}
